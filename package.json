{
  "name": "tauri-loadcell-driver",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "lint": "npm-run-all lint:*",
    "lint:prettier": "prettier --check .",
    "autoformat": "npm-run-all autoformat:*",
    "autoformat:prettier": "prettier --write src/ *.json *.ts *.html"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.1",
    "preact": "^10.5.15"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.1.5",
    "@tauri-apps/cli": "^1.0.2",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.0.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  }
}
